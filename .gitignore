# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*

# next.js
/.next/
/out/
next-env.d.ts

# production build
/build
/dist

# testing / coverage
/coverage
/test-results
/playwright-report
/.playwright

# env — .env.example 만 추적한다.
# 개별 이름을 나열하면 새 파일(.env.production 등)이 조용히 추적돼 비밀값이 커밋된다.
.env
.env.*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo

# supabase local dev
/supabase/.branches
/supabase/.temp

# os / editor noise
.DS_Store
Thumbs.db
*.pem
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.idea/
*.swp

# Claude Code local settings (never commit)
.claude/

# Local Codex project settings (never commit)
/.codex/

# Design decision records — portfolio / local only, git-untracked by rule
docs/design-decisions/

# 테스트 도구 산출물
/.playwright-mcp/
/.qa-reports/

# playwright storageState (세션 재사용) — 커밋하지 않는다
playwright/.auth/
